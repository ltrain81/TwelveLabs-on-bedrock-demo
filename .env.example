# Video Understanding PoC - Environment Configuration Template
# Copy this file to .env and configure your values

# =============================================================================
# DEPLOYMENT CONFIGURATION
# =============================================================================

# AWS Region for deployment (us-east-1 recommended for S3 Vectors support)
AWS_DEFAULT_REGION=us-east-1
CDK_DEFAULT_REGION=us-east-1

# CORS Origins (comma-separated list)
# Add your frontend domains here
CORS_ORIGINS=http://localhost:3000,https://localhost:3000,https://yourdomain.com

# =============================================================================
# AWS ACCOUNT CONFIGURATION (Optional - auto-detected if not set)
# =============================================================================

# Your AWS Account ID (auto-detected from AWS CLI if not set)
# Get with: aws sts get-caller-identity --query Account --output text
# AWS_ACCOUNT_ID=123456789012

# =============================================================================
# TESTING CONFIGURATION
# =============================================================================

# API Gateway URL (set automatically by deploy.sh)
# API_BASE_URL=https://your-api-id.execute-api.region.amazonaws.com/prod

# AWS Region for testing (usually same as deployment region)
# AWS_REGION=us-east-1

# =============================================================================
# FRONTEND CONFIGURATION 
# =============================================================================

# React App API URL (set in frontend/.env by deploy.sh)
# REACT_APP_API_URL=https://your-api-id.execute-api.region.amazonaws.com/prod

# =============================================================================
# LAMBDA RUNTIME CONFIGURATION (Auto-set by CDK - DO NOT MODIFY)
# =============================================================================

# These are automatically configured by the CDK deployment:
# VIDEO_BUCKET=video-understanding-{account-id}-{region}
# OPENSEARCH_ENDPOINT=https://collection-endpoint.region.aoss.amazonaws.com
# REGION={deployment-region}
# CLOUDFRONT_DOMAIN={distribution-domain}
# CORS_ORIGIN={first-cors-origin}

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================

# 1. Basic deployment (uses defaults):
#    ./deploy.sh

# 2. Custom region deployment:
#    export AWS_DEFAULT_REGION=us-west-2
#    ./deploy.sh

# 3. Production deployment with custom domain:
#    export AWS_DEFAULT_REGION=us-east-1
#    export CORS_ORIGINS=https://app.mydomain.com,https://mydomain.com
#    ./deploy.sh

# 4. Testing after deployment:
#    source .env.test
#    python test_final_api.py

# =============================================================================
# SUPPORTED REGIONS
# =============================================================================

# Twelve Labs Models: Check AWS Bedrock console for availability
# S3 Vectors: Limited regions (us-east-1 recommended)
# OpenSearch Serverless: Most AWS regions
# CDK/CloudFormation: All AWS regions